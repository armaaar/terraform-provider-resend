---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "resend_webhooks Data Source - terraform-provider-resend"
subcategory: ""
description: |-
  Lists every Resend webhook in the account. The list endpoint omits signing_secret — fetch each webhook via data.resend_webhook if you need it.
---

# resend_webhooks (Data Source)

Lists every Resend webhook in the account. The list endpoint omits `signing_secret` — fetch each webhook via `data.resend_webhook` if you need it.

## Example Usage

```terraform
terraform {
  required_version = ">= 1.5"

  required_providers {
    resend = {
      source  = "armaaar/resend"
      version = "~> 1.0"
    }
  }
}

provider "resend" {}

# List every webhook in the account. The signing_secret is intentionally
# missing from this list endpoint — fetch each webhook via data.resend_webhook
# if you need it.
data "resend_webhooks" "all" {}

output "disabled_webhooks" {
  description = "Endpoints of webhooks currently in disabled state."
  value       = [for w in data.resend_webhooks.all.webhooks : w.endpoint if w.status == "disabled"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `webhooks` (Attributes List) All webhooks, in the order Resend returned them. (see [below for nested schema](#nestedatt--webhooks))

<a id="nestedatt--webhooks"></a>
### Nested Schema for `webhooks`

Read-Only:

- `created_at` (String)
- `endpoint` (String)
- `events` (List of String)
- `id` (String)
- `status` (String)

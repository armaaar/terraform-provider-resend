---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "resend_domains Data Source - terraform-provider-resend"
subcategory: ""
description: |-
  Lists every Resend sending domain in the account. The list endpoint omits per-domain records, capabilities and tls — fetch each domain via data.resend_domain if you need those.
---

# resend_domains (Data Source)

Lists every Resend sending domain in the account. The list endpoint omits per-domain `records`, `capabilities` and `tls` — fetch each domain via `data.resend_domain` if you need those.

## Example Usage

```terraform
terraform {
  required_version = ">= 1.5"

  required_providers {
    resend = {
      source  = "armaaar/resend"
      version = "~> 1.0"
    }
  }
}

provider "resend" {}

# List every Resend domain in the account — handy for inventory dashboards or
# enforcing fleet-wide policies.
data "resend_domains" "all" {}

output "verified_domains" {
  description = "Names of all domains that Resend has fully verified."
  value       = [for d in data.resend_domains.all.domains : d.name if d.status == "verified"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `domains` (Attributes List) All domains, in the order Resend returned them. (see [below for nested schema](#nestedatt--domains))

<a id="nestedatt--domains"></a>
### Nested Schema for `domains`

Read-Only:

- `click_tracking` (Boolean)
- `created_at` (String)
- `id` (String)
- `name` (String)
- `open_tracking` (Boolean)
- `region` (String)
- `status` (String)
- `tracking_subdomain` (String)
